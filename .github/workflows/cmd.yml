name: testCMD
on: check_suite

jobs:
  job1:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1
    - name: Build
      shell: cmd
      run: |
         call build.bat
         IPCONFIG
         SYSTEMINFO 
   
