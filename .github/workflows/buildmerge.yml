name: matrix test

on: check_suite
    #branches: 
      #- master

jobs:

  build-normal:
    runs-on: ubuntu-latest
    timeout-minutes: 600
    steps:
      - name: logging
        run: echo "$GITHUB_REF"
        
  build-extra:
    runs-on: ubuntu-latest
    steps:
      - name: logging2
        run: echo "$GITHUB_REF"
  
  test:
    strategy:
      matrix:
        type: [normal, extra]
    runs-on: ubuntu-latest
    needs: [build-normal, build-extra]
    #needs: build-${{ matrix.type }}
    steps:
      - name: logging3
        run: echo "$GITHUB_REF"
   
