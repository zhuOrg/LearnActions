name: matrix test

on: pull_request
    #branches: 
      #- master

jobs:

  build-normal:
    runs-on: ubuntu-latest
    timeout-minutes: 600
    steps:
      - name: logging
        run: echo "$GITHUB_REF"
        
  build-extra:
    runs-on: ubuntu-latest
    steps:
      - name: logging2
        run: echo "$GITHUB_REF"

