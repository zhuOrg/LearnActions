
name: pull requestmerge

on:  push
    
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: current branch
      run: |
   
        echo  $LABELS
      env:
        LABELS: ${{ toJson(github.event.pull_request.labels) }}
      
